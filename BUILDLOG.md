# vt — BUILDLOG

Append entries below as tasks ship via `/ship <ID>`. Each task is anchored with
`<!-- TASK:<ID>:START -->` and `<!-- TASK:<ID>:END -->` for O(1) slicing via
`python3 .claude/ritual/scripts/slice-buildlog.py <ID>`.

---
